name: ci

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    runs-on: ubuntu-22.04
    permissions:
      # Required for the release step below; harmless on PR/manual runs.
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          # Full history so `git tag --list` sees prior releases on main.
          fetch-depth: 0

      - uses: actions/setup-go@v6
        with:
          go-version: '1.24'
          cache: true

      - name: install bpf toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y clang llvm libbpf-dev linux-tools-common linux-tools-generic ipset
          # bpftool ships under linux-tools-`uname -r` on ubuntu-22.04 runners.
          BPFTOOL=$(ls /usr/lib/linux-tools-*/bpftool | head -n1)
          sudo ln -sf "$BPFTOOL" /usr/local/bin/bpftool

      # ----- Steps that run on every event (PR, push, manual) -----

      - run: make generate
      - run: go vet ./...
      - run: go test ./... -count=1

      - name: bench smoke
        # Just confirms benchmarks still compile and produce output. No perf budget.
        run: go test -run='^$' -bench=BenchmarkPipeline -benchtime=2x -benchmem ./internal/pipeline/

      - run: make build

      # ----- Release steps. Only run on push to main. -----

      - name: compute next semver version and release-worthiness
        id: version
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          set -euo pipefail
          # Find latest strict-semver tag (vMAJOR.MINOR.PATCH, no suffixes).
          latest=$(git tag --list 'v[0-9]*.[0-9]*.[0-9]*' --sort=-v:refname \
                   | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' \
                   | head -1 || true)
          if [ -z "$latest" ]; then
            version="0.0.1"
            range="HEAD"
          else
            base="${latest#v}"
            IFS='.' read -r major minor patch <<< "$base"
            patch=$((patch + 1))
            version="${major}.${minor}.${patch}"
            range="${latest}..HEAD"
          fi

          # Only release when the new commits include something user-impacting.
          # Conventional-commits prefixes that warrant a release: feat, fix,
          # perf, refactor. Docs, ci, chore, style, test, build merges do not
          # cut a new .deb.
          release_re='^(feat|fix|perf|refactor)(\(.+\))?!?:'
          if git log --format='%s' "$range" | grep -qE "$release_re"; then
            release=true
            echo "Release-worthy commits since ${latest:-<initial>}:"
            git log --format='  %h %s' "$range" | grep -E "$release_re" || true
          else
            release=false
            echo "No feat/fix/perf/refactor commits since ${latest:-<initial>}; skipping release."
            echo "Commits in range:"
            git log --format='  %h %s' "$range" || true
          fi

          {
            echo "version=${version}"
            echo "tag=v${version}"
            echo "release=${release}"
          } >> "$GITHUB_OUTPUT"
          echo "Next release: v${version} (after ${latest:-<no prior tags>})  release=${release}"

      - name: package .deb
        if: github.event_name == 'push' && github.ref == 'refs/heads/main' && steps.version.outputs.release == 'true'
        run: VERSION=${{ steps.version.outputs.version }} make package

      - name: checksums
        if: github.event_name == 'push' && github.ref == 'refs/heads/main' && steps.version.outputs.release == 'true'
        run: sha256sum dns2ipset_*_amd64.deb > SHA256SUMS

      - name: create release
        if: github.event_name == 'push' && github.ref == 'refs/heads/main' && steps.version.outputs.release == 'true'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: ${{ steps.version.outputs.tag }}
          generate_release_notes: true
          files: |
            dns2ipset_*_amd64.deb
            SHA256SUMS
          fail_on_unmatched_files: true
